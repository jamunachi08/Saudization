from setuptools import setup, find_packages

setup(
    name="saudization_dashboard",
    version="0.1.0",
    description="AlphaX Saudization Dashboard (Frappe Cloud ready)",
    author="AlphaX",
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
    install_requires=[],
)
